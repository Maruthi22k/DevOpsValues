apiVersion: v1
kind: Pod
metadata:
  name: resource-pod
  environment: dev
spec:
  containers:
    - name: resource-container
      image: nginx
      resources:
        limits:
          memory: "128Mi"
          cpu: "500m"
        requests:
          memory: "64Mi"
          cpu: "250m"