- name: ansible filters using dynamic variables read
  hosts: local
  connection: local

  vars:
    topics: linux, ansible, python, docker

  tasks:
    - name: ansible filters using dynamic variables read 
      ansible.builtin.debug:
        msg: "topics are {{ topics.split(',') | list}}"