- name: priyarity of variables
  hosts: local
  connection: local
  vars:
    name: "This is playbook variable"
  vars_files:
    - bookDetail.yaml
  vars_prompt:
    - name: NAME
      prompt: "i am apromt varible"
      private: no
  tasks:
    - name: print names
      vars:
        name: "i am task variable"
      ansible.builtin.debug:
        msg: "my name is {{NAME}}"



#### this playbook command to execute
#### cd DevOpsValues/Ansible/ ---  ansible-playbook -i inventory.ini 06-priyarityVariables.yaml