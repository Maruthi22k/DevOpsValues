- name: Condition check inAnsible
  hosts: local
  connection: local
  vars:
    IS_LIVE: true
    Number_of_condtions: 10
  tasks:
    - name: check the condition
      ansible.builtin.debug:
        msg: "this is live condsitions check"
      when: IS_LIVE == true
  tasks:
    - name: check the condition
      ansible.builtin.debug:
        msg: "this is live condsitions out check"
      when: IS_LIVE == false

  tasks:
    - name: number of conditions check
      ansible.builtin.debug:
        msg: "Number of conditions is {{ Number_of_condtions }}"
      when: Number_of_condtions > 5

  tasks:
    - name: number of conditions less than 5
      ansible.builtin.debug:
        msg: "Number of condtion is less then {{ Number_of_condtions }}"
      when: Number_of_condtions < 5

## condition check using 'when ' keyword

## Command to execute-- ansible-playbook -i inventory.ini 08-conditionCheck.yaml