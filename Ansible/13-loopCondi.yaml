- name: Install packages
  hosts: local
  connection: local
  become: yes

  tasks:
    - name: Install web and database packages
      ansible.builtin.package:
        name: "{{ item }}"
        state: present
      loop:
        - httpd
        - nginx
        - mariadb-server
