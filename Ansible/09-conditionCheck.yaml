- name: Condition chec ansible_facts
  hosts: local
  connection: local
  tasks:
    - name: get data from using ansible_facts
      ansible.builtin.debug:
        msg: "full data == {{ansible_facts}}" 
        
    - name: display OS and version
      ansible.builtin.debug:
        msg: "The OS is {{ ansible_distribution }} and the version is {{ ansible_distribution_version }}"